# url-ingestion-rag-bot
A chatbot that can ingest any given URL and recursively scrape relevant content from linked pages, then use Retrieval-Augmented Generation (RAG) to answer user questions accurately based on the collected content. Have minimal latency and robust handling of structured and unstructured data.
